

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Vilcan_Andrea_Lab7.ShopPage"
             Title="Shop">

    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Enter shop name"
                    Margin="20"
                    Text="{Binding ShopName}"
                    HeightRequest="50" />

            <Editor Placeholder="Enter shop address"
                    Margin="20"
                    Text="{Binding Adress}"
                    HeightRequest="50" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Text="Save Shop"
                        Grid.Column="0"
                        MaximumWidthRequest="200"
                        Clicked="OnSaveButtonClicked" />

                <Button Text="Show on Map"
                        Grid.Column="1"
                        MaximumWidthRequest="200"
                        Clicked="OnShowMapButtonClicked" />
            </Grid>

            <!-- sarcina laborator: delete shop -->
            <Button Text="Delete Shop"
                    Margin="0,20,0,0"
                    Clicked="OnDeleteButtonClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>